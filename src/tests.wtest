import juego.*
import bomber.*
import direcciones.*

describe "tests del juego"{
	method initialize(){
		
	}
	test "mover, si puede, los bombermans en todas las direcciones" {

		assert.that(true)

	}
}